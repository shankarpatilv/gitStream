apiVersion: 1

providers:
  - name: GitStream
    orgId: 1
    folder: GitStream
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
